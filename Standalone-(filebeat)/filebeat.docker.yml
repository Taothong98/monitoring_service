# filebeat.inputs:
# - type: log
#   enabled: true
#   paths:
#     - /var/log/*.log       
#     # - /var/log/*.log      

# output:
#   elasticsearch:
#     hosts: ["elasticsearch:9200"]

# filebeat.config:
#   modules:
#     path: ${path.config}/modules.d/*.yml
#     reload.enabled: false

# --------------------------------------------------------------

filebeat.inputs:
- type: log
  enabled: true
  paths:
    # - /var/log/ex/*.log       
    - /var/log/*.log      

output:
  elasticsearch:
    hosts: ["elasticsearch:9200"]

# output.logstash:
#   # The Logstash hosts
#   hosts: ["logstash:5044"]

filebeat.config:
  modules:
    path: ${path.config}/modules.d/*.yml
    reload.enabled: false

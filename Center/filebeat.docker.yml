# filebeat.inputs:
# - type: syslog
#   format: rfc3164
#   protocol.udp:
#     host: "0.0.0.0:9004"     

# output.logstash:
#   # The Logstash hosts
#   hosts: ["logstash:5044"]

# filebeat.config:
#   modules:
#     path: ${path.config}/modules.d/*.yml
#     reload.enabled: false


# --------------------------------------------------------------

filebeat.inputs:
- type: log
  enabled: true
  paths:
    # - /var/log/ex/*.log       
    - /var/log/*.log      

output.logstash:
  # The Logstash hosts
  hosts: ["logstash:5044"]

filebeat.config:
  modules:
    path: ${path.config}/modules.d/*.yml
    reload.enabled: false
